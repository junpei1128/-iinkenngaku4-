# メール送信機能テストガイド

このガイドでは、メール送信機能をテストするための具体的な手順を説明します。

## 前提条件

- サーバーとフロントエンドが起動していること
- Gmailアカウントを持っていること
- Gmailで2段階認証が有効になっていること

## ステップ1: Gmailアプリパスワードの取得

### 1-1. 2段階認証の確認・有効化

1. Googleアカウントにログイン: https://myaccount.google.com/
2. 左メニューから **セキュリティ** をクリック
3. **2段階認証プロセス** を確認
   - 既に有効になっている場合: ステップ1-2に進む
   - 無効の場合: **2段階認証プロセス** をクリックして有効化（手順に従って設定）

### 1-2. アプリパスワードの生成

1. Googleアカウントの **セキュリティ** ページにいることを確認
2. **2段階認証プロセス** の下にある **アプリパスワード** をクリック
   - 2段階認証を有効にした直後は、少し時間がかかって表示される場合があります
3. **アプリを選択** ドロップダウンから **その他（カスタム名）** を選択
4. 名前を入力（例: `医院見学レポートシステム`）
5. **生成** をクリック
6. **重要**: 表示された16文字のパスワードをコピーしてください（この画面でしか表示されません）
   - 形式: `xxxx xxxx xxxx xxxx`（スペースを含む16文字）
   - 実際に使用する際は、スペースを削除して16文字の文字列として使用します

## ステップ2: 環境変数の設定

### 2-1. .envファイルの編集

1. `server` ディレクトリに移動
2. `.env` ファイルを開く（テンプレートから作成済み、または `env.template` からコピー）
3. 以下の値を実際の値に置き換える：

```env
PORT=3001
FRONTEND_URL=http://localhost:5185
GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=xxxxxxxxxxxxxxxx
```

- `GMAIL_USER`: あなたのGmailアドレスを入力（例: `your-email@gmail.com`）
- `GMAIL_APP_PASSWORD`: ステップ1-2で取得した16文字のアプリパスワードを貼り付け
  - **注意**: スペースは削除して、16文字の連続した文字列として入力してください

### 2-2. 依存関係のインストール

Gmail対応に必要なパッケージをインストールします：

```bash
cd server
npm install
```

### 2-3. サーバーの再起動

1. 現在実行中のサーバーを停止（Ctrl+C）
2. サーバーディレクトリで以下を実行：

```bash
cd server
npm run dev
```

3. コンソールに警告メッセージが表示されないことを確認：
   - ✅ 正常: `サーバーが起動しました: http://localhost:3001`
   - ❌ エラー: `警告: GMAIL_USERまたはGMAIL_APP_PASSWORDが設定されていません` → `.env`ファイルを確認

## ステップ3: アプリケーションでのテスト

### 3-1. ログイン

1. ブラウザで http://localhost:5185/ にアクセス
2. ナビゲーションバーから **「通知先登録」** をクリック
3. ログイン画面で以下を入力：
   - **ID**: `yamashita-j@consuldent.jp`
   - **パスワード**: `jyunpei1128`
4. **「ログイン」** をクリック

### 3-2. 通知先の登録

1. **「通知先登録」** ページに移動（ログイン後自動的に移動）
2. **「＋通知先を追加」** ボタンをクリック
3. テスト用のメールアドレスを登録：
   - **名前**: `テストユーザー`（任意）
   - **メールアドレス**: **実際にメールを受信できるアドレス**（Gmail、Outlookなど）
4. **「保存」** をクリック
5. **「保存済み」** と表示されることを確認

### 3-3. レポートの作成と完了

1. ナビゲーションバーから **「レポート一覧」** をクリック
2. **「＋新規作成」** ボタンをクリック
3. 必須項目を入力：
   - **見学日**: 任意の日付（例: 2024-01-15）
   - **見学先医院名**: `テスト医院`
   - **都道府県**: 任意（例: 東京都）
   - **市**: `テスト市`
4. その他の項目は任意で入力
5. ページ下部の **「完了」** ボタンをクリック
6. 成功メッセージが表示されることを確認：
   - ✅ 成功: `レポートを完了しました。\n○件のメールを送信しました`

### 3-4. メールの確認

1. 登録したメールアドレスの受信トレイを確認
2. 迷惑メールフォルダも確認（初回送信の場合、迷惑メールに入る可能性があります）
3. メールの内容を確認：
   - **件名**: 「医院見学レポート完了のお知らせ」
   - **送信者**: `.env`で設定した`GMAIL_USER`のアドレス
   - **本文**: 
     - レポート情報（医院名、見学日など）
     - アプリケーションURL（クリック可能なリンク）

## ステップ4: エラーケースのテスト

### 4-1. 通知先未登録の場合

1. 通知先登録ページで、すべての通知先を削除
2. レポート一覧に戻り、レポートを完了
3. 以下のメッセージが表示されることを確認：
   - `レポートを完了しました。\n通知先が登録されていないため、メールは送信されませんでした。`

### 4-2. アプリパスワードが無効な場合

1. `.env`ファイルの`GMAIL_APP_PASSWORD`を間違った値に変更（例: `invalid_password`）
2. サーバーを再起動
3. レポートを完了
4. エラーメッセージが表示されることを確認（レポートは正常に完了）：
   - `レポートを完了しました。\nただし、メール送信に失敗しました: メール送信に失敗しました`

### 4-3. 通常のGmailパスワードを使用した場合

1. `.env`ファイルの`GMAIL_APP_PASSWORD`を通常のGmailパスワードに変更
2. サーバーを再起動
3. レポートを完了
4. エラーメッセージが表示されることを確認：
   - `Invalid login` などのエラーがサーバーログに表示される
   - **解決方法**: アプリパスワードを正しく生成して設定する

## ステップ5: 複数通知先のテスト

### 5-1. 複数の通知先を登録

1. 通知先登録ページで、複数のメールアドレスを登録（例: 3件）
2. 各メールアドレスに **「保存済み」** と表示されることを確認

### 5-2. レポート完了

1. レポートを完了
2. 成功メッセージに **「○件のメールを送信しました」** と表示されることを確認（例: `3件のメールを送信しました`）
3. 登録したすべてのメールアドレスにメールが届くことを確認

## 確認チェックリスト

- [ ] Gmailで2段階認証が有効になっている
- [ ] アプリパスワードが生成できた
- [ ] `.env`ファイルが正しく設定できた
- [ ] サーバーが警告なしで起動できた
- [ ] 通知先が登録できた
- [ ] レポート完了時にメールが送信された
- [ ] メールが正しく受信できた
- [ ] エラーハンドリングが正しく動作した

## トラブルシューティング

### メールが届かない場合

1. **迷惑メールフォルダを確認**
   - 初回送信の場合、迷惑メールに入る可能性があります

2. **サーバーのログを確認**
   - サーバーのコンソールでエラーメッセージがないか確認
   - エラーがある場合は、エラーメッセージを確認

3. **アプリパスワードが正しいか確認**
   - 16文字の文字列が正しく設定されているか
   - 通常のGmailパスワードではなく、アプリパスワードを使用しているか
   - スペースが含まれていないか

4. **2段階認証が有効か確認**
   - Googleアカウントのセキュリティ設定で確認
   - アプリパスワードを使用するには、2段階認証が必須です

5. **Gmailの送信制限を確認**
   - 個人アカウント: 1日500通まで
   - 制限に達した場合は、翌日まで待つ必要があります

### サーバーエラーが発生する場合

1. **`.env`ファイルの構文を確認**
   - 改行が正しいか
   - 引用符が不要か（値に引用符は不要）
   - スペースがないか（特にアプリパスワードの前後）

2. **サーバーを再起動**
   - サーバーを停止して再起動
   - 環境変数が正しく読み込まれるか確認

3. **ブラウザの開発者ツールで確認**
   - F12キーで開発者ツールを開く
   - **Network** タブでAPIリクエストを確認
   - エラーレスポンスがないか確認

### アプリパスワードが無効と表示される場合

1. **アプリパスワードが正しくコピーされているか確認**
   - 前後の空白がないか
   - すべての16文字が含まれているか
   - スペースが含まれていないか

2. **通常のGmailパスワードを使用していないか確認**
   - アプリパスワードは通常のパスワードとは異なります
   - Googleアカウントのセキュリティページで再生成してください

3. **2段階認証が有効か確認**
   - アプリパスワードを使用するには、2段階認証が必須です

## 次のステップ

テストが完了したら、本番環境での使用を開始できます。本番環境では：

- Gmailの送信制限（1日500通）を考慮した運用計画
- メール送信の監視とログの確認
- エラーハンドリングの改善（必要に応じて）

## 注意事項

- **送信制限**: Gmail個人アカウントは1日500通まで送信可能です。このシステムの使用頻度では問題になりにくいですが、大量送信が必要な場合はGoogle Workspaceアカウントの使用を検討してください。
- **送信元アドレス**: 送信元アドレスはGmailアカウントのアドレスに固定されます。
- **セキュリティ**: アプリパスワードは機密情報です。`.env`ファイルをGitにコミットしないよう注意してください。
